package com.lmccrone.pomodoro.app;

import com.lmccrone.pomodoro.data.PomodoroTime;

import static org.junit.Assert.assertTrue;
import org.junit.Test;

public class PomodoroTest {
    /*@Test(expected = IllegalArgumentException.class)
    public void TestPomodoroTimeSetTimeNegativeMinutes() {
        PomodoroTime testTime = new PomodoroTime("test", -1, 0);
    }
    
    @Test(expected = IllegalArgumentException.class)
    public void TestPomodoroTimeSetTimeTooManyMinutes() {
        PomodoroTime testTime = new PomodoroTime("test", 61, 0);
    }
    
    @Test(expected = IllegalArgumentException.class)
    public void TestPomodoroTimeSetTimeNegativeSeconds() {
        PomodoroTime testTime = new PomodoroTime("test", 0, -1);
    }
    
    @Test(expected = IllegalArgumentException.class)
    public void TestPomodoroTimeSetTimeTooManySeconds() {
        PomodoroTime testTime = new PomodoroTime("test", 0, 60);
    }
    
    @Test(expected = IllegalArgumentException.class)
    public void TestPomodoroTimeSetTimeZero() {
        PomodoroTime testTime = new PomodoroTime("test", 0, 0);
    }

    @Test
    public void TestPomodoroTimeSetTimeValid() {
        PomodoroTime testTime = new PomodoroTime("test", 60, 0);
        assertTrue("Time is valid", true);
    }*/
}
